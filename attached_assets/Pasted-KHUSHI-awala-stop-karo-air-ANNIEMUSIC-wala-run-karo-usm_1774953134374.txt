KHUSHI awala stop karo air ANNIEMUSIC wala run karo usmai stats aur ping wala grp mai work nahi kar raha hai usko fix karna khushi k reference le k wo bug fix karo
The user wants to:
Stop KHUSHI Bot workflow
Start ANNIEMUSIC workflow
Fix stats and ping commands in ANNIEMUSIC that don't work in groups - reference KHUSHI's implementation
Let me first check the workflows and understand what's needed. I need to:
Stop KHUSHI Bot
Start/create ANNIEMUSIC workflow
Fix ANNIEMUSIC stats and ping for group use
Let me check the workflows skill and also look at how KHUSHI's stats and ping work in groups vs ANNIEMUSIC's implementation.
Pehle workflows skill padh leta hoon, phir KHUSHI vs ANNIEMUSIC ping/stats compare karta hoon:
Loaded workflows skill
$ cat ANNIEMUSIC/plugins/tools/ping.py; echo "===KHUSHI==="; cat KHUSHI/plugins/ping.py
$ head -50 ANNIEMUSIC/plugins/tools/stats.py; echo "===KHUSHI==="; head -50 KHUSHI/plugins/stats.py
Now I have a clear picture. Let me understand the problem:
Stop KHUSHI Bot → Switch to ANNIEMUSIC workflow
ANNIEMUSIC ping in groups - The current ANNIEMUSIC ping uses @language decorator and _["ping_2"] string + reply_photo. The issue might be that in groups, the ping photo doesn't send (due to some restriction) or the command doesn't respond.
Looking at KHUSHI's ping, it uses a raw API send (client.invoke with raw_func.messages.SendMedia) to send the photo with spoiler, plus fallbacks. This is more robust.
ANNIEMUSIC stats in groups - The stats plugin relies on reply_video but videos might face permission issues in groups, especially when combined with inline keyboards. The user's complaint about group functionality suggests the same underlying problem: ANNIEMUSIC's reply methods aren't handling group restrictions the way KHUSHI's direct API calls do.
Looking at the ping implementations side by side, ANNIEMUSIC uses the @language decorator and reply_photo with a PNG image, while KHUSHI bypasses these abstractions entirely with raw API calls and includes message deletion. The real issue might be that ANNIEMUSIC's approach relies on helper methods that don't account for group-specific permission constraints, whereas KHUSHI's lower-level implementation gives it more control over how the message gets sent.
Now examining the actual filters and decorators — both commands use nearly identical filter chains, but ANNIEMUSIC's @language decorator could be silently failing in groups where the language setting isn't configured. The stats command likely has the same decorator issue, which would explain why neither command responds at all in group chats. The telegraph URL might work or might not depending on the platform's restrictions. For the ping issue in groups, I'm realizing the @language decorator is likely the culprit—if the group hasn't registered a language in the database, the decorator probably fails silently and the command never executes. I need to either remove the decorator entirely, hardcode the response text like KHUSHI does, and use raw API calls for better compatibility, or at least make the language lookup optional so the function still runs as a fallback.
Now let me check why ANNIEMUSIC ping/stats fail in groups — check the @language decorator:
$ grep -n "def language\b\|async def language" ANNIEMUSIC/utils/decorators/language.py | head -5; grep -A 20 "^def language" ANNIEMUSIC/utils/decorators/language.py | head -25
$ cat ANNIEMUSIC/plugins/tools/ping.py
Now I understand the issue clearly. Looking at the ANNIEMUSIC ping:
It uses @language decorator which is fine (it has a try/except that defaults to "en")
It uses _["ping_2"] string from the language file - this string might not exist or might not work
It uses reply_photo without spoiler
But looking at the KHUSHI ping which works, the key difference is:- KHUSHI uses raw API (client.invoke with raw_func.messages.SendMedia) to send the photo in groups- This is because in groups, sometimes media with inline keyboards fails if sent via regular methods- KHUSHI's approach is more robust

The real problem is likely that reply_photo with an InlineKeyboardMarkup fails in certain groups, and the fallback reply_text also doesn't work reliably. KHUSHI avoids this entirely by using the raw API to send media directly, which handles group restrictions better, while ANNIEMUSIC relies on higher-level methods that sometimes fail.
Now I'm checking whether ANNIEMUSIC's stats plugin has the same issue — the reply_video method with inline keyboards might not work in groups either, so I need to see how KHUSHI handles video sending to apply the same pattern. see how it sends the stats message. From checking KHUSHI's stats.py, it uses its own _stats_keyboard function, so let me see how it handles stats in groups too.
The core issue is probably that the telegra.ph video URL doesn't work reliably with Pyrogram's video send in groups. KHUSHI's stats doesn't use video at all—just text with an inline keyboard. The @language decorator might also be causing problems. So I should rewrite both: ping needs the KHUSHI treatment (no decorator, hardcoded caption, raw API photo send with spoiler), and stats should drop the video send entirely and use a text-based approach with the keyboard instead. The real problem is likely that reply_photo fails in groups due to missing bot permissions or the image URL timing out, and then reply_text also fails silently, leaving no response at all. The from config import * is probably not the culprit, but I should rewrite ANNIEMUSIC's ping to be more robust like KHUSHI's version, and fix the stats command to avoid using video URLs since Pyrogram might not handle telegra.ph links properly in that context. The stats decorator already uses @language, so that's not the issue there.
Clearly samjha. ANNIEMUSIC ping aur stats dono mein issue hai:
reply_photo/reply_video groups mein silently fail hota hai
KHUSHI ka raw API approach zyada reliable hai
Pehle workflows switch karta hoon, phir dono fix karta hoon:
Executed action
ANNIEMUSIC
Removed configuration for KHUSHI Bot
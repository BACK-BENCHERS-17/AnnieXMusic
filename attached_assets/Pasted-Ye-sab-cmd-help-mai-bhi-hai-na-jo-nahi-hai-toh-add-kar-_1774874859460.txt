Ye sab cmd help mai bhi hai na jo nahi hai toh add kar do

Aur language bhi work nahi kar raha hai ping stats acha nahi dikh raha hai loading bar hatao aur ANNIEMUSIC mai jo jo chiz hai Ismai wo sab lagao magar best tarike se aur best ui mai aur jo emoji id chaiye rahega bata dena mai de dunga

The user wants several things:

All new commands added to the help menu too
Language command doesn't work (need to add /language command)
Ping and stats don't look good - remove loading bar and improve UI
Add everything from ANNIEMUSIC that's not in KHUSHI, with best UI
Need custom emoji IDs - will provide them
Let me first look at the current state of things:

Help menu in start.py - what commands are listed

Language plugin - check if it exists

Ping and stats plugins - current UI

What ANNIEMUSIC has that KHUSHI doesn't yet

Now I'm examining the files to understand the current implementation across all these areas.

Pehle sab kuch dekh leta hoon ek saath:

Ab help menu, ANNIEMUSIC language plugin aur baaki files padh leta hoon:

Ab helpers.py aur help inline file padh leta hoon:

Ab ANNIEMUSIC ping, stats aur language plugin padh leta hoon:

Let me check the strings directory more carefully.

Now I know:1. There are 5 languages: ar, en, hi, ru, tr2. Language strings exist (lang_1, lang_2, lang_3, lang_4)3. The settings menu has a "LANGUAGE_SETTINGS" button but no handler for it4. I need to create a language plugin that handles:- /language command (admins only)

LANGUAGE_SETTINGS callback
Language picker with buttons for each available language5. Ping/Stats UI - user wants to remove loading bar from ping.py. In stats.py, the loading bar is only in the system view. Let me check the user's complaint more carefully.
The user wants me to remove the loading bar completely from both ping and stats displays, showing just the raw numbers instead. Both ANNIEMUSIC and KHUSHI use the same loading bar style in their stats, so I need to strip that out entirely.

Now I'm checking the settings.py file to see what callbacks are already handled - I can see various toggle and view callbacks for search mode, play type, auth mode, and voting, but the LANGUAGE_SETTINGS callback isn't implemented yet. I need to create a language plugin that handles this callback along with a /language command, and then fix the ping.py file to remove the loading bar display.

Now I have a clear picture of everything. Let me do all the changes needed:1. Create language plugin (KHUSHI/plugins/language.py):

/language command (admin only) — opens language picker
LANGUAGE_SETTINGS callback — same picker from settings
set_lang_<code> callback — actually set the language2. Fix ping.py — Remove loading bars, just show raw numbers as plain text
Fix stats.py — Remove loading bars from all views, show raw numbers
For the language plugin, I need to:- Get all available languages from strings.languages_present- Build an inline keyboard with language options

Use set_lang from database to save the language per group (chat_id)
Let me check how set_lang works and what languages_present contains to understand the structure.

Ab sab changes ek saath karta hoon:

Now I have all the information I need. Let me:1. Create KHUSHI/plugins/language.py with the language command and callback2. Fix KHUSHI/plugins/ping.py - remove loading bars
3. Fix KHUSHI/plugins/stats.py - remove loading bars

For language plugin - available languages: en (🇺🇸 English), ar (🇸🇦 العربية), hi (🇮🇳 हिन्दी), ru (🇷🇺 Русский), tr (🇹🇷 Türkçe)The language is set per-chat (set_lang(chat_id, lang)). The command should be admin-only.For ping - just remove the _bar() function usage and show plain CPU/RAM/DISK numbers.

For stats, I'll do the same thing - strip out the loading bars from the system view and keep the box layout clean. Now I'm ready to implement all these changes.

Sabse pehle language plugin create karta hoon, phir ping aur stats fix karta hoon parallel mein:

Now let me fix ping.py and stats.py - remove loading bars from both. I'll rewrite them with clean UI.

Ab ping aur stats fix karta hoon:

Now I need to also check if the help menu already contains the language section (HELP_10 already has /language in helpers.py). Let me verify.

Looking at helpers.py, HELP_10 already contains:

<b>• /language</b>        ↳ ᴄʜᴀɴɢᴇ ʙᴏᴛ ʟᴀɴɢᴜᴀɢᴇ
Good, so the help menu is already complete with all commands including language.

Now let me also check if there are any remaining ANNIEMUSIC features that should be in KHUSHI but aren't. From ANNIEMUSIC:

content_guard.py - content guard toggle command (the DB functions are in KHUSHI already)
mode247.py - /247 is in controls.py ✅
volume.py - /volume is in controls.py ✅
autoleave.py - need to check
seeker.py - need to check
Let me look at what content_guard, autoleave, and seeker actually do.

Ab ANNIEMUSIC ke remaining plugins bhi dekh leta hoon:

Now I see what I need to do:1. content_guard - /nsfw and /gnsfw commands need to be added to KHUSHI as a plugin. The functions content_guard_on, content_guard_off, is_content_guard_on, is_global_nsfw_off, set_global_nsfw_off, set_global_nsfw_on should be in KHUSHI's database.

autoleave/auto_end - These are background tasks. I need to check if KHUSHI already has them.

seeker - Background timer. I need to check if KHUSHI already has this.Let me check:

$ grep -rn "auto_leave\|auto_end\|timer\|seeker\|content_guard_on\|content_guard_off\|is_global_nsfw" KHUSHI/ | grep -v __pycache__ | grep "\.py:" | head -20
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.

You're out of credits. 
